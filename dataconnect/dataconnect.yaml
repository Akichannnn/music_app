specVersion: "v1"
serviceId: "deployweb"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "deployweb-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
